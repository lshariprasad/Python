"""Since We Can Use this Online Command"""
# print('x'*x_count) - Remove 7 to 10 Lines Add this Only

numbers = [5,2,5,2,2]

for x_count in numbers:
        output = ''
        for count in range (x_count):
            output += 'x'
        print(output)